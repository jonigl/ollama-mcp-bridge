{
  "mcpServers": {
    "weather": {
      "command": "uv",
      "args": [
        "--directory",
        "mcp-servers-config",
        "run",
        "mock-weather-mcp-server.py"
      ],
      "env": {
        "MCP_LOG_LEVEL": "ERROR"
      }
    }
  }
}
